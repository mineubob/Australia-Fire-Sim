[package]
name = "fire-sim-ffi"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
fire-sim-core = { path = "../core" }
lazy_static = "1.4"

[lib]
crate-type = ["cdylib", "staticlib"]
